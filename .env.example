# Database Connection URL (SQLite for local zero-config, or PostgreSQL for production)
DATABASE_URL="file:./dev.db"

# For PostgreSQL (e.g. Supabase, Neon, Render):
# DATABASE_URL="postgresql://user:password@host:5432/onefi_db?schema=public"
